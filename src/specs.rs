//! This module contains
//! - the external specification `cap_map`, used for pre/post conditions
//! - the refined specification `link_map`, used by the `state` implementation

pub mod cap_map;
pub mod link_map;
